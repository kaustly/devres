<section class="posts">
  <h1>Posts</h1>

  <div class="center">
    <% @posts.each do |post| %>
      <h2><%= post.title %></h2>
      <%= link_to 'Show Post', post_path(post) %> |
      <%= link_to 'Edit', edit_post_path(post) %>
    <% end %>
  </div>

</section>
